const score = 5; // Aqui foi declarada uma variável chamada score que recebe o valor de 5
const highestScore = 8; // Aqui foi declarada outa variável chamada highestScore que recebe o valor de 8

console.log(`A pontuação é ${score}`); // Aqui é impresso a string 'A pontuação é' seguido do valor da variável score (5)
console.log(`A pontuação mais alta é ${highestScore}`); // // Aqui é impresso a string 'A pontuação mais alta é' seguido do valor da variável highestScore (8)