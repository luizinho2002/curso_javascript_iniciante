let num1; // Aqui foi declarada uma variável chamada num1 que não foi inicializada.
let num2; // Aqui foi ideclarada uma variável chamada num2 que não foi inicializada.

num1 = 8; // Aqui inicializamos a variável num1 com o valor 8
num2 = 15; // Aqui inicializamos a variável num2 com o valor 15

console.log('8 == 15 é', (num1 == num2)); // Aqui é verificado e impresso no console do navegador se num1 é igual a num2.
console.log('8 != 15 é', (num1 != num2)); // Aqui é verificado e impresso no console do navegador se num1 é diferente de num2.
console.log('8 > 15 é', (num1 > num2)); // Aqui é verificado e impresso no console do navegador se num1 é maior que num2.
console.log('8 < 15', (num1 < num2)); // Aqui é verificado e impresso no console do navegador se num1 é menor que num2.
console.log('8 >= 15', (num1 >= num2)); // Aqui é verificado e impresso no console do navegador se num1 é maior ou igual a num2.
console.log('8 <= 15', (num1 <= num2)); // Aqui é verificado e impresso no console do navegador se num1 é menor ou igual a num2.

// Esses valores retornam valores booleanos - true or false

/*
    Observe, que como estamos calculando diretamente o resultado, e não armazenando-o na variável, colocamos-o entre
    parênteses().
*/