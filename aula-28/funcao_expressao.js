// Cria uma variável chamada 'language' e atribui a ela uma função
let language = function() {

    // Quando essa função for chamada, ela vai executar essa linha:
    console.log('JavaScript'); // Imprime a mensagem 'JavaScript' no console
}

// Chama a função armazenada em 'language'
language();